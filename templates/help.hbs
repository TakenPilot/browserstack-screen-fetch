Queries BrowserStack API:
  Example: chelsea-market --user aaa --password bbb --filename example-definition.json
  --help: help documentation
  --json: returns result as JSON
  --status: get api status
  --list: get all browser and device combinations
  --latest: get all latest browser and device combinations
  --kill-all: kill all active workers
  --url http://google.com: get a url and take a screenshot
  --wait-interval=1000: set timeout interval when waiting for workers
  --wait-to-load=1000: set time interval before taking screenshot
  --kill=false: set to false to not kill workers after fetching screenshots
